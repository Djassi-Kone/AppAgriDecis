import 'package:flutter/material.dart';

class AppColors {
  static const primaryGreen = Color(0xFF4CAF50);     // vert principal (bouton, accents)
  static const darkGreen   = Color(0xFF388E3C);
  static const lightGreen  = Color(0xFF8BC34A);
  static const textGrey    = Color(0xFF757575);
  static const backgroundOverlay = Colors.black54;   // pour assombrir l'image de fond
}